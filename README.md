# EZCook

This app is built with Next.js v13 and Feathers.js

## Development FLow

Run Database with docker:

```sh
cd ./packages/db
docker compose up
```

Run apps parallel:

```sh
pnpm run --parallel -r dev
```
